<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PerksByDaylightV2.Survivors"
             BackgroundColor="#2b2b2b">

    <!--<controls:AnimationPage.PageAnimation>
        <controls:SlidePageAnimation Duration="Short" Type="Fade" Subtype="Default"/>
    </controls:AnimationPage.PageAnimation>-->


    <ListView x:Name="MyListView"
            ItemsSource="{Binding Items}"
            ItemSelected="MyListView_ItemSelected"
            CachingStrategy="RecycleElement">
        <!--Built in Cells-->



        <ListView.ItemTemplate>
            <DataTemplate>
                <ImageCell ImageSource="{Binding CharacterPicture}" Text="{Binding CharacterName}" TextColor="#f2f2f2"  />
            </DataTemplate>
        </ListView.ItemTemplate>

        <!--Custom View Cells-->
        <!--
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout>
            <Label Text="{Binding Text}" 
                   Style="{DynamicResource ListItemTextStyle}" />
            <Label Text="{Binding Detail}" 
                   Style="{DynamicResource ListItemDetailTextStyle}"/>
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
    -->
    </ListView>
</ContentPage>